apiVersion: v1
kind: Service
metadata:
  name: {{ include "tomato-chart.fullname" . }}
  labels:
    {{- include "tomato-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tomato-chart.selectorLabels" . | nindent 4 }}
