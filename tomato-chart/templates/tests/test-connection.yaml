apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tomato-chart.fullname" . }}-test-connection"
  labels:
    {{- include "tomato-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tomato-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
